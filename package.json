{
  "name": "user-management-system",
  "workspaces": [
    "packages/client",
    "packages/server"
  ],
  "scripts": {
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server"
  }
}
